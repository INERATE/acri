{
  "name": "acri-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal TypeScript port of acri's core resolver (corpus + compass) -- v0.1 scope only. Ahead of decisions.md's own gate for this language -- see README.md.",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist/test/compass.test.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
